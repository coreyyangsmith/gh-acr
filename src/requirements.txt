# Dependencies moved to the repository root.
# Prefer:
#   uv sync
#   uv sync --extra analysis
#   uv sync --extra local-llm
#   uv sync --extra dev
#
# See ../pyproject.toml and ../requirements.txt
