# Dependencies are managed with uv from pyproject.toml (see uv.lock).
# Install:
#   uv sync
#   uv sync --extra analysis
#   uv sync --extra local-llm
#   uv sync --extra dev
#
# Run scripts:
#   uv run python -m src.cli.run_all --help
